# 🚀 快速开始 - 3步获取APK

## 📋 准备工作

你已经完成：
- ✅ 项目代码已准备
- ✅ Git仓库已初始化
- ✅ GitHub Actions已配置
- ✅ 所有冗余文件已清理

## 🎯 接下来只需3步

### 第1步：创建GitHub仓库

1. 访问 https://github.com/new
2. 仓库名称：`iot-face`（或任意名称）
3. 设为 Public（公开）或 Private（私有）
4. **不要**勾选任何初始化选项（README、.gitignore等）
5. 点击 `Create repository`

### 第2步：推送代码到GitHub

复制GitHub显示的命令，或直接运行：

```bash
cd /Users/zfj/CodeBuddy/iot-face

# 添加远程仓库（替换为你的GitHub用户名）
git remote add origin https://github.com/你的用户名/iot-face.git

# 推送代码
git push -u origin main
```

如果提示需要登录，按照提示操作即可。

### 第3步：等待自动构建完成

1. 推送完成后，访问你的GitHub仓库
2. 点击顶部的 `Actions` 标签
3. 看到 "Build Android APK" 正在运行 🔄
4. 等待5-10分钟，直到显示 ✅

### 第4步：下载APK

**方式A：从Releases下载（推荐）**
1. 点击仓库的 `Releases` 标签
2. 点击最新的 Release
3. 下载 `app-debug.apk`

**方式B：从Actions下载**
1. 在 `Actions` 页面点击已完成的workflow
2. 向下滚动找到 `Artifacts`
3. 下载 `app-debug`
4. 解压得到APK

---

## 📱 安装到电视

1. 将 `app-debug.apk` 复制到U盘
2. U盘插入电视
3. 用文件管理器找到并安装
4. 完成！🎉

---

## 🔧 修改后端地址

如果需要修改后端IP：

1. 编辑 `.env.production` 文件：
   ```env
   VITE_SSE_URL=http://新的IP:端口/api/sse/connect
   ```

2. 提交并推送：
   ```bash
   git add .env.production
   git commit -m "更新后端地址"
   git push
   ```

3. GitHub Actions会自动构建新的APK
4. 从Releases下载更新后的APK

---

## 💡 提示

- **首次构建**：需要10分钟左右（下载依赖）
- **后续构建**：3-5分钟（有缓存）
- **APK大小**：约10-15MB
- **有效期**：Artifacts保存30天，Releases永久保存

---

## 🆘 遇到问题？

### 推送失败？
```bash
# 如果遇到认证问题，使用Personal Access Token
# 1. GitHub Settings > Developer settings > Personal access tokens
# 2. Generate new token (classic)
# 3. 勾选 repo 权限
# 4. 生成token后，推送时用token作为密码
```

### GitHub Actions没有运行？
- 检查仓库的 `Actions` 标签是否启用
- 确认代码已推送到 `main` 或 `master` 分支

### 找不到Releases？
- Release仅在推送到 `main` 或 `master` 时自动创建
- 可以从 `Actions` 的 Artifacts 下载

---

## 📞 下一步

现在你可以：

1. ✅ **推送代码到GitHub** ← 从这里开始
2. ⏳ 等待自动构建
3. 📥 下载APK
4. 📱 安装到电视
5. 🎉 开始使用！

**推送命令**：
```bash
git remote add origin https://github.com/你的用户名/iot-face.git
git push -u origin main
```

祝你使用愉快！🚀
